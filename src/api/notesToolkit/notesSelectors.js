const notesList = (state) => state.notesList.notesList;
const isActiveNote = (state) => state.notesList.isActiveList;
const fetchHelper = (state) => state.notesList.fetchHelper;
export { notesList, isActiveNote, fetchHelper };
